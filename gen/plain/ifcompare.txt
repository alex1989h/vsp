			if(MyXML.testSignatur(xml, %s, namespace+".%s", %s)){
				if (this.oldId < (int)xml.getParamValues()[0]) {
					this.oldId = (int)xml.getParamValues()[0];
					%s r = model.%s(%s);
					receiver.send(MyXML.createMethodResponse((int)this.oldId,r).getBytes());
					continue;
				}
			}