	@Override
	public %s %s(%s) {
		System.out.format(%s);
		String str = MyXML.createMethodCall(%s,Namespace.getName()+".%s", %s);
		byte[] reply = sender.send(str.getBytes());
		return (%s)MyXML.createXML(reply).getParamValues()[1];
	}
	